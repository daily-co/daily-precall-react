{
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"start:app": "yarn build:library && yarn workspace apps/daily-precall-starterkit watch",
		"start:library": "yarn workspace packages/daily-precall-react watch",
		"build:app": "yarn build:lib && yarn workspace apps/daily-precall-starterkit build",
		"build:library": "yarn workspace packages/daily-precall-react build"
	},
	"devDependencies": {
		"vite": "latest"
	}
}
